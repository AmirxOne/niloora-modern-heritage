"use client";

/**
 * @deprecated از `@/components/inputs` (TextBox) استفاده کنید.
 * این فایل فقط برای سازگاری با importهای قدیمی نگه داشته شده است.
 */
export { default as Input } from "@/components/inputs/TextBox";
